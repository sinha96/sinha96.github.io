source "https://rubygems.org"

# Mirrors what GitHub Pages runs server-side.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end

# Lock these for compatibility on macOS / newer Rubies.
gem "webrick", "~> 1.8"
